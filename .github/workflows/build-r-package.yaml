name: Build mice

on:
  workflow_dispatch:
  push:

jobs:
  build:
    name: Build mice
    permissions:
      checks: write
      contents: read
    uses: Displayr/nixr-public/.github/workflows/build-flake-installable.yaml@master
    with:
      installable-path: pkgs.x86_64-linux.rPackages.mice
      build-args: --override-input displayr-mice .
      installable-name: mice
    secrets: inherit